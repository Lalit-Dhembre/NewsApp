<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieRatingsApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryVariant">@color/purple</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.MovieRatingsApp" parent="Base.Theme.MovieRatingsApp" />

    <style name="Widget.AppCompat.RatingBar.Indicator" parent="Widget.Material3.BottomNavigationView">
        <item name="android:color">@color/yellow</item>
    </style>

</resources>